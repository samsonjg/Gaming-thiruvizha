// Identifiers for the Gaming Thiruvizha 2026 demo event/poll — the first
// event on this platform, not a special case in the code (see
// docs/AI_CONTEXT.md). Once the event/poll exist in Firestore (created via
// the admin UI — see docs/DEVELOPMENT.md), these are only used by the
// stand-in Kyn event page (KynEventPage.tsx) to link to the right poll.
export const GT_POLL_SLUG = 'gaming-thiruvizha-2026'
